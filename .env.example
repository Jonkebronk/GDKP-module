# ===========================================
# GDKP Platform Environment Variables
# ===========================================

# Database (Railway provides DATABASE_URL automatically when you add PostgreSQL)
DATABASE_URL="postgresql://gdkp:gdkp_secret@localhost:5432/gdkp?schema=public"

# Redis (Railway provides REDIS_URL automatically when you add Redis)
REDIS_URL="redis://localhost:6379"

# Discord OAuth
# Create app at: https://discord.com/developers/applications
DISCORD_CLIENT_ID=""
DISCORD_CLIENT_SECRET=""
DISCORD_REDIRECT_URI="http://localhost:3000/api/auth/discord/callback"

# PayPal API
# Create app at: https://developer.paypal.com/dashboard/applications
PAYPAL_CLIENT_ID=""
PAYPAL_CLIENT_SECRET=""
PAYPAL_MODE="sandbox"  # Change to "live" for production
PAYPAL_WEBHOOK_ID=""

# JWT Authentication
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# App URLs
# Update these with your Railway URLs in production
API_URL="http://localhost:3000"
FRONTEND_URL="http://localhost:5173"

# Exchange Rates (gold per currency unit)
GOLD_PER_EUR=1000
PLATFORM_FEE_PERCENT=5

# ===========================================
# Railway Production Notes:
# ===========================================
# 1. DATABASE_URL and REDIS_URL are auto-provided by Railway plugins
# 2. Set DISCORD_REDIRECT_URI to: https://your-api-url.railway.app/api/auth/discord/callback
# 3. Set API_URL to your Railway API service URL
# 4. Set FRONTEND_URL to your Railway web service URL
# 5. Change PAYPAL_MODE to "live" and use production PayPal credentials
